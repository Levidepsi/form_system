let mongoose;_8a1‍.x([["default",()=>_8a1‍.o]]);_8a1‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const userModel = new mongoose.Schema(
	{
		name: {
			type: String,
			required: true,
			trim: true
		},
		email: {
			type: String,
			required: true,
			trim: true,
			unique: true
		},
		password: {
			type: String,
			required: true,
			min: 6,
			max: 64
		},
		phoneNumber: {
			type: String,
			required: true,
			trim: true,
			unique: true
		},
		isAdmin: {
			type: Boolean,
			required: true,
			default: 0
		}
	},
	{ timestamps: true }
);

// userModel.path("email").validate(async email => {
// 	const countEmail = await mongoose.models.User.countDocuments({ email });
// 	return !countEmail;
// });

const User = mongoose.model("User", userModel);

_8a1‍.d(User);
